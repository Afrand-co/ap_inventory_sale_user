<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Read-only view for Stock Quantities (Stock on Hand) for Sales users -->
    <record id="view_stock_quant_readonly_sales" model="ir.ui.view">
        <field name="name">stock.quant.readonly.sales</field>
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.view_stock_quant_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="attributes">
                <attribute name="edit">false</attribute>
                <attribute name="create">false</attribute>
                <attribute name="delete">false</attribute>
                <attribute name="groups">sales_team.group_sale_salesman</attribute>
            </xpath>
        </field>
    </record>

    <!-- Read-only form view for Stock Quantities for Sales users -->
    <record id="view_stock_quant_form_readonly_sales" model="ir.ui.view">
        <field name="name">stock.quant.form.readonly.sales</field>
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.view_stock_quant_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="attributes">
                <attribute name="edit">false</attribute>
                <attribute name="create">false</attribute>
                <attribute name="delete">false</attribute>
                <attribute name="groups">sales_team.group_sale_salesman</attribute>
            </xpath>
        </field>
    </record>

    <!-- Read-only view for Stock Moves for Sales users -->
    <record id="view_stock_move_readonly_sales" model="ir.ui.view">
        <field name="name">stock.move.readonly.sales</field>
        <field name="model">stock.move</field>
        <field name="inherit_id" ref="stock.view_move_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="attributes">
                <attribute name="edit">false</attribute>
                <attribute name="create">false</attribute>
                <attribute name="delete">false</attribute>
                <attribute name="groups">sales_team.group_sale_salesman</attribute>
            </xpath>
        </field>
    </record>

    <!-- Read-only form view for Stock Moves for Sales users -->
    <record id="view_stock_move_form_readonly_sales" model="ir.ui.view">
        <field name="name">stock.move.form.readonly.sales</field>
        <field name="model">stock.move</field>
        <field name="inherit_id" ref="stock.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="attributes">
                <attribute name="edit">false</attribute>
                <attribute name="create">false</attribute>
                <attribute name="delete">false</attribute>
                <attribute name="groups">sales_team.group_sale_salesman</attribute>
            </xpath>
        </field>
    </record>

    <!-- Read-only view for Stock Pickings for Sales users -->
    <record id="view_stock_picking_readonly_sales" model="ir.ui.view">
        <field name="name">stock.picking.readonly.sales</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.vpicktree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="attributes">
                <attribute name="edit">false</attribute>
                <attribute name="create">false</attribute>
                <attribute name="delete">false</attribute>
                <attribute name="groups">sales_team.group_sale_salesman</attribute>
            </xpath>
        </field>
    </record>

    <!-- Read-only form view for Stock Pickings for Sales users -->
    <record id="view_stock_picking_form_readonly_sales" model="ir.ui.view">
        <field name="name">stock.picking.form.readonly.sales</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="attributes">
                <attribute name="edit">false</attribute>
                <attribute name="create">false</attribute>
                <attribute name="delete">false</attribute>
                <attribute name="groups">sales_team.group_sale_salesman</attribute>
            </xpath>
        </field>
    </record>
</odoo>
