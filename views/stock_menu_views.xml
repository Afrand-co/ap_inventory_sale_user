<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Model Access for Stock Quant (Stock on Hand) -->
    <record id="access_stock_quant_sale_user" model="ir.model.access">
        <field name="name">stock.quant for sale own user</field>
        <field name="model_id" ref="stock.model_stock_quant"/>
        <field name="group_id" ref="sales_team.group_sale_salesman"/>
        <field name="perm_read" eval="1"/>
        <field name="perm_write" eval="0"/>
        <field name="perm_create" eval="0"/>
        <field name="perm_unlink" eval="0"/>
    </record>

    <!-- Model Access for Stock Move -->
    <record id="access_stock_move_sale_user" model="ir.model.access">
        <field name="name">stock.move for sale own user</field>
        <field name="model_id" ref="stock.model_stock_move"/>
        <field name="group_id" ref="sales_team.group_sale_salesman"/>
        <field name="perm_read" eval="1"/>
        <field name="perm_write" eval="0"/>
        <field name="perm_create" eval="0"/>
        <field name="perm_unlink" eval="0"/>
    </record>

    <!-- Model Access for Stock Picking -->
    <record id="access_stock_picking_sale_user" model="ir.model.access">
        <field name="name">stock.picking for sale own user</field>
        <field name="model_id" ref="stock.model_stock_picking"/>
        <field name="group_id" ref="sales_team.group_sale_salesman"/>
        <field name="perm_read" eval="1"/>
        <field name="perm_write" eval="0"/>
        <field name="perm_create" eval="0"/>
        <field name="perm_unlink" eval="0"/>
    </record>

    <!-- Model Access for Product Product -->
    <record id="access_product_product_sale_user" model="ir.model.access">
        <field name="name">product.product for sale own user</field>
        <field name="model_id" ref="product.model_product_product"/>
        <field name="group_id" ref="sales_team.group_sale_salesman"/>
        <field name="perm_read" eval="1"/>
        <field name="perm_write" eval="0"/>
        <field name="perm_create" eval="0"/>
        <field name="perm_unlink" eval="0"/>
    </record>

    <!-- Model Access for Stock Location -->
    <record id="access_stock_location_sale_user" model="ir.model.access">
        <field name="name">stock.location for sale own user</field>
        <field name="model_id" ref="stock.model_stock_location"/>
        <field name="group_id" ref="sales_team.group_sale_salesman"/>
        <field name="perm_read" eval="1"/>
        <field name="perm_write" eval="0"/>
        <field name="perm_create" eval="0"/>
        <field name="perm_unlink" eval="0"/>
    </record>

    <!-- Give menu access to Inventory/Reporting/Stock -->
    <!-- Note: We'll use a more generic approach to ensure menu visibility -->
</odoo>
